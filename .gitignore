# Python
__pycache__/
*.py[cod]
*.pyd
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenv
.venv/
venv/
ENV/

# Logs / local outputs
output/
data_exports/
monitor_data/
config/autosave/
*.log
wandb/

# Studio runtime data (SQLite, user configs, task logs)
studio_data/

# Studio frontend (Node deps and build artifacts)
studio/web/node_modules/
studio/web/dist/
studio/web/.vite/

# 顶层意外创建的 node_modules（vitest / 某些工具可能在 repo root 拉一份）
/node_modules/

# Studio 走 preset 池（studio_data/presets/，已被 studio_data/ 总规则覆盖），
# 旧的 config/ 目录已删，不再需要 user yaml 排除规则。

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
.claude/

# Caches (HF/Transformers etc.)
**/.cache/


# Model weights (keep code/config, ignore large weights)
*.safetensors
*.ckpt
*.bin
*.pt
*.pth
*.npz
*.onnx
models/text_encoders/tokenizer.json
models/text_encoders/vocab.json
models/wd14/
models/taeflux/
models/cltagger/

# Dataset (training images)
dataset/

# Misc
nul
# Root-level AI agent files — gitignored so contributors can put personal
# config / @docs/AGENTS.md import here without conflicting with the repo.
# Project 公约 lives at docs/AGENTS.md (NOT ignored — note the leading /).
/AGENTS.md
/CLAUDE.md

# 本地解压的设计稿原始目录（已整理进 mockup/）
1/

# 工具生成的临时目录
.spec-workflow/

# Self-update 进程间通信用的瞬时标志（server 写 → cli.py / wrapper 读后立即删）
tmp/

# 文档本地草稿（个人 TODO / 未定稿设计 / 临时笔记）
docs/_local/

# Local UI snapshots before redesign (not for commit)
_ui_backup/

# 一键启动器的产物（tools/build_launcher.py 编出来的）。
#
# `AnimaLoraStudio.exe`（Windows）**故意不在此列** —— 它随仓库一起提交，
# 这样用户 `git clone` 下来就能直接双击，不用先去 Releases 找附件。
# 由 .github/workflows/publish-launcher.yml 在 windows runner 上构建并回提。
#
# 非 Windows 的产物仍然忽略：Linux/macOS 用户直接 `./studio.sh` 就行，
# 不需要引导程序，提交进去只是给每个人多下几 MB。
/AnimaLoraStudio
/dist/

# 第三方库缓存（pip / HuggingFace / torch / npm / ...）—— 见
# studio/infrastructure/local_cache.py：默认收进项目文件夹，免得几个 GB
# 悄悄堆在系统盘上。整个目录随时可删，删了只是下次重下。
/.cache/
